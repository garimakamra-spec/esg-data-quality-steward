{
  "name": "scim-role-adaptive-onboarding",
  "version": "1.0.0",
  "description": "SCIM-Driven Role-Adaptive Workspace Provisioning and Enterprise Onboarding Engine",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --loader ts-node/esm --test tests/*.test.ts || npx tsx --test tests/*.test.ts",
    "test:run": "tsx --test tests/*.test.ts",
    "start": "tsx src/server.ts"
  },
  "keywords": [
    "scim",
    "enterprise",
    "onboarding",
    "rbac",
    "provisioning"
  ],
  "author": "Antigravity Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.15.0",
    "typescript": "^5.4.5"
  }
}
